// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: "https://victorperezasuaje.github.io/entolab-technologies",
    base: "/entolab-technologies"
});
